

const OptionDivide = ({ title }) => {
    return (
        <div className={`
            w-full flex items-center justify-start border-b border-slate-400
            text-base text-left font-medium text-slate-400 pb-1
        `}>
            {title}
        </div>
    );
}

export default OptionDivide;